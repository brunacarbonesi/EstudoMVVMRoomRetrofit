<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brunacarbonesi.apps.estudomvvmroomretrofit">

    <application
        android:name=".MovieApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstudoMVVMRoomRetrofit">
        <activity
            android:name=".aboutAppActivity.AboutAppActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"/>
        <activity
            android:name=".movieslistActivity.MoviesListActivity"
            android:exported="true"
            android:theme="@style/Theme.EstudoMVVMRoomRetrofit.NoActionBar" />
        <activity android:name=".splashActivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>